# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Lokale config (credentials, token, state, lock) - niet in repo
config/

# Project-definities bevatten paden, e-mails en whitelist van derden;
# committeer alleen config.toml.example als template.
config.toml

# Lokale logs
logs/
